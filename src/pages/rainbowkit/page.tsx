export default function Page() {
  //   RainbowKit
  // 描述：使用 RainbowKit 库，基于 wagmi 和 viem，与 MetaMask 等钱包交互，提供连接钱包、签名、交易及链切换功能，内置美观的 UI 组件（如 ConnectButton）。
  // 特点：用户友好，提供可定制的连接按钮和模态框，支持多钱包（如 MetaMask、WalletConnect），与 Next.js 集成简单，适合快速构建 dApp。
  return (
    <div className="grid grid-rows-[20px_1fr_20px] items-center justify-items-center min-h-screen p-8 pb-20 gap-16 sm:p-20 font-[family-name:var(--font-geist-sans)]">
      <h1>RainbowKit!</h1>
    </div>
  );
}
